# ── NOTA ──────────────────────────────────────────────────────────────────────
# Este archivo era usado para variables de desarrollo local SIN prefijo VITE_.
# Vite IGNORA cualquier variable sin el prefijo VITE_, por lo que estas
# variables nunca llegaron al bundle del navegador.
#
# Las credenciales MQTT correctas con prefijo VITE_ están ahora en .env
# ──────────────────────────────────────────────────────────────────────────────