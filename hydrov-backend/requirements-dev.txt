# requirements-dev.txt — dependencias SOLO para desarrollo y testing
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# SQLite async para tests (evita Docker en CI)
aiosqlite>=0.20.0

# Coverage
pytest-cov>=5.0.0

# Linting
ruff>=0.4.0
mypy>=1.9.0
